# WordLoop

Приложение для изучения словарных слов методом интервального повторения на базе
модифицированного SM-2. Поддерживает уроки, список слов, озвучку и режим
случайного изучения без изменения данных.

## Возможности
- Уроки с тремя состояниями: вопрос, ответ, нет слов на сегодня.
- Озвучка слова через TextToSpeech, ожидание инициализации с индикатором.
- Проверка ответа с подсветкой ошибок (правильно/ошибка/пропуск).
- Список всех слов с добавлением, редактированием и удалением.
- Цветовая индикация статуса слова в списке.
- Режим случайного изучения без обновления БД.

## Алгоритм интервального повторения
Используется SuperMemo SM-2 с упрощенной оценкой качества ответа (q = 0 или 5).

- Формула пересчета интервала: `I(n) = I(n-1) * ef`
- Новый коэффициент легкости:
  `ef' = ef + (0.1 - (5 - q) * (0.08 + (5 - q) * 0.02))`
- Нижняя граница коэффициента: `ef >= 1.3`

Правила обновления карточки:
- `q = 0`:
  - статус `PROGRESS_RESET`;
  - `ef` пересчитывается только при первом сбросе;
  - интервал = 1 сутки;
  - `next_review_date = now + 10 минут`.
- `q = 5` при статусах `NEW` или `PROGRESS_RESET`:
  - `ef` не пересчитывается;
  - интервал = 1 сутки;
  - `next_review_date = now + 1 день`.
- `q = 5` при статусе `IN_PROGRESS`:
  - пересчет `ef`, интервала и даты следующего повтора.

## Экран урока
Состояния:
- **Вопрос** — озвучка слова, ввод ответа, проверка.
- **Ответ** — показывается правильное слово и подсветка ошибок.
- **Нет слов** — сообщение и кнопка перехода в режим случайного изучения.

Особенности:
- Автофокус на поле ввода и открытие клавиатуры.
- Скрытие клавиатуры на экране ответа.
- Кнопка проверки отключена до инициализации TTS.
- Подсветка ответа основана на выравнивании строк (редакционное расстояние).

## Экран списка слов
Показывает все слова в алфавитном порядке.

Цвет фона строки:
- мягкий красный: `PROGRESS_RESET`;
- мягкий зеленый: повтор сегодня или в прошлом;
- мягкий синий: дата повтора в будущем.

После слова отображается количество дней до следующего повтора.

## Режим случайного изучения
Доступен, когда слова на сегодня закончились.

Поведение:
- выбирается случайное слово из БД;
- при проверке ответов карточки не обновляются;
- переход на следующее слово также случайный.

## Архитектура и ключевые файлы

```
app/src/main/java/com/example/wordcheck/
├── data/
│   ├── WordEntity.kt
│   ├── WordDao.kt
│   ├── WordDatabase.kt
│   └── WordsRepository.kt
├── domain/
│   ├── CardStatus.kt
│   └── Sm2Scheduler.kt
└── ui/
    ├── LessonScreen.kt
    ├── WordListScreen.kt
    ├── WordCheckApp.kt
    └── WordsViewModel.kt
```

## Иконка
Иконка приложения задается в `app/src/main/res/drawable/wordcheck_icon.xml`.

## Ручной деплой в Google Play (шпаргалка)

Ниже чеклист ручного релиза в Google Play, чтобы не вспоминать шаги каждый раз.

### 1) Подготовка версии

1. В `app/build.gradle` увеличьте:
   - `versionCode` (обязательно: больше предыдущего),
   - `versionName` (по необходимости, например `1.0.4`).
2. Проверьте, что приложение собирается локально.
3. Зафиксируйте изменения в Git (commit/tag по вашему процессу).

### 2) Подготовка подписи

1. Убедитесь, что в корне проекта есть `keystore.properties`.
2. Проверьте, что в `keystore.properties` корректные поля:
   - `storeFile`
   - `storePassword`
   - `keyAlias`
   - `keyPassword`
3. Убедитесь, что файл keystore (`.jks`) доступен по пути из `storeFile`.

Пример `keystore.properties`:

```properties
storeFile=/absolute/path/to/release.jks
storePassword=***
keyAlias=***
keyPassword=***
```

### 3) Сборка release AAB

Вариант через терминал:

```bash
./gradlew :app:clean :app:bundleRelease
```

Готовый файл:
`app/build/outputs/bundle/release/app-release.aab`

Вариант через Android Studio:
`Build` -> `Generate Signed Bundle / APK` -> `Android App Bundle`.

### 4) Загрузка в Google Play Console

1. Откройте [Google Play Console](https://play.google.com/console/).
2. Выберите приложение.
3. Перейдите: `Testing` -> `Internal testing` (или нужный трек).
4. Нажмите `Create new release`.
5. Загрузите `app-release.aab`.
6. Заполните release notes.
7. Нажмите `Review release` -> `Start rollout to Internal testing`.

### 5) Проверка после выкладки

1. Убедитесь, что релиз появился в выбранном треке.
2. Проверьте, что отображается нужный `versionCode`.
3. Обновите приложение на тестовом устройстве и проверьте запуск.

### Частые проблемы

- Ошибка `version code ... has already been used`:
  увеличьте `versionCode` и пересоберите `.aab`.
- Ошибка подписи:
  проверьте `keystore.properties`, путь к `.jks`, alias и пароли.
- Загружен не тот build:
  убедитесь, что загружаете файл из `app/build/outputs/bundle/release/`.

